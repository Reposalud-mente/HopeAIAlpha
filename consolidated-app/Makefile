# Makefile para tareas comunes

install:
	pnpm install

lint:
	pnpm lint

test:
	pnpm test || echo "(Asegúrate de tener pruebas configuradas)"

build:
	pnpm build

start:
	pnpm start

dev:
	pnpm dev

prisma-migrate:
	pnpm prisma:migrate

prisma-seed:
	pnpm prisma:seed
